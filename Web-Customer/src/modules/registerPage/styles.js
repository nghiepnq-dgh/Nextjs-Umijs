import {
	primaryColor,
	secondaryColor,
	defaultColor,
	lightBlueColor,
	warningColor,
	//successColor,
	infoColor,
	errorColor,
} from '../../theme.js';

const RegisterPageStyles = {
    
}

export default RegisterPageStyles
