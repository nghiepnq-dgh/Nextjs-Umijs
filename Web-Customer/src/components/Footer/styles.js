import {
	primaryColor,
	secondaryColor,
	defaultColor,
	lightBlueColor,
	warningColor,
	//successColor,
	infoColor,
	errorColor,
} from '../../theme'

const footerStyles = {
    copyright: {
        textAlign: 'center',
        width: '100%',
        position: 'absolute',
        bottom: '0',
        color: primaryColor,
    }
}

export default footerStyles;