import {
	primaryColor,
	secondaryColor,
	defaultColor,
	lightBlueColor,
	warningColor,
	//successColor,
	infoColor,
    errorColor,
} from '../../theme'
import { rest } from 'lodash';

const headerStyles = {
    root: {
		flexGrow: 1,
	  },
	title: {
		flexGrow: 1,
	},
	button: {
		color: '#6dcef5',
	}
}

export default headerStyles;